name = "hheuristics-articles"
main = "src/index.ts"
compatibility_date = "2024-09-01"

# Add your Cloudflare account ID after you create the Worker, or via `wrangler init`
# account_id = "YOUR_ACCOUNT_ID"

[ai]
binding = "AI"

[triggers]
# Run every day at 09:00 UTC (adjust as needed)
crons = ["0 9 * * *"]


